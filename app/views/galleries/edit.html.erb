<%= render 'navigation'%>
<%= form_for @gallery, :html => { :multipart => true } do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<%= @gallery.errors[:title].first unless @gallery.errors[:title].blank? %>
<br/>
<%= f.label :content %>
<%= f.text_area :content, cols: 20, rows: 10 %>
<%= @gallery.errors[:content].first unless @gallery.errors[:content].blank? %>
<br/>
<%= f.label :picture %>
<%= f.file_field :picture %></p>

<%= f.submit:submit %>
<% end %>